/*
 * x402api Public API
 * Public External-Wallet x402 integration endpoints for programmatic charges, readiness, receiving addresses, resources, payments, receipts, and facilitator discovery.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.x402api.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.x402api.client.model.FeePolicyModeInputEnum;
import com.x402api.client.model.FeePolicyQuoteCurrencyInputEnum;
import com.x402api.client.model.NetworkFeePreviewPrice;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import com.x402api.client.core.JSON;

/**
 * NetworkFeePreview
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.24.0")
public class NetworkFeePreview {
  public static final String SERIALIZED_NAME_FEE_MODE = "fee_mode";
  @SerializedName(SERIALIZED_NAME_FEE_MODE)
  @javax.annotation.Nullable
  private FeePolicyModeInputEnum feeMode;

  public static final String SERIALIZED_NAME_QUOTE_CURRENCY = "quote_currency";
  @SerializedName(SERIALIZED_NAME_QUOTE_CURRENCY)
  @javax.annotation.Nullable
  private FeePolicyQuoteCurrencyInputEnum quoteCurrency;

  public static final String SERIALIZED_NAME_FEE_ALLOWANCE_CAP_QUOTE_MICROS = "fee_allowance_cap_quote_micros";
  @SerializedName(SERIALIZED_NAME_FEE_ALLOWANCE_CAP_QUOTE_MICROS)
  @javax.annotation.Nullable
  private String feeAllowanceCapQuoteMicros = "0";

  public static final String SERIALIZED_NAME_PRICES = "prices";
  @SerializedName(SERIALIZED_NAME_PRICES)
  @javax.annotation.Nonnull
  private List<NetworkFeePreviewPrice> prices = new ArrayList<>();

  public NetworkFeePreview() {
  }

  public NetworkFeePreview feeMode(@javax.annotation.Nullable FeePolicyModeInputEnum feeMode) {
    this.feeMode = feeMode;
    return this;
  }

  /**
   * Get feeMode
   * @return feeMode
   */
  @javax.annotation.Nullable
  public FeePolicyModeInputEnum getFeeMode() {
    return feeMode;
  }

  public void setFeeMode(@javax.annotation.Nullable FeePolicyModeInputEnum feeMode) {
    this.feeMode = feeMode;
  }


  public NetworkFeePreview quoteCurrency(@javax.annotation.Nullable FeePolicyQuoteCurrencyInputEnum quoteCurrency) {
    this.quoteCurrency = quoteCurrency;
    return this;
  }

  /**
   * Get quoteCurrency
   * @return quoteCurrency
   */
  @javax.annotation.Nullable
  public FeePolicyQuoteCurrencyInputEnum getQuoteCurrency() {
    return quoteCurrency;
  }

  public void setQuoteCurrency(@javax.annotation.Nullable FeePolicyQuoteCurrencyInputEnum quoteCurrency) {
    this.quoteCurrency = quoteCurrency;
  }


  public NetworkFeePreview feeAllowanceCapQuoteMicros(@javax.annotation.Nullable String feeAllowanceCapQuoteMicros) {
    this.feeAllowanceCapQuoteMicros = feeAllowanceCapQuoteMicros;
    return this;
  }

  /**
   * Get feeAllowanceCapQuoteMicros
   * @return feeAllowanceCapQuoteMicros
   */
  @javax.annotation.Nullable
  public String getFeeAllowanceCapQuoteMicros() {
    return feeAllowanceCapQuoteMicros;
  }

  public void setFeeAllowanceCapQuoteMicros(@javax.annotation.Nullable String feeAllowanceCapQuoteMicros) {
    this.feeAllowanceCapQuoteMicros = feeAllowanceCapQuoteMicros;
  }


  public NetworkFeePreview prices(@javax.annotation.Nonnull List<NetworkFeePreviewPrice> prices) {
    this.prices = prices;
    return this;
  }

  public NetworkFeePreview addPricesItem(NetworkFeePreviewPrice pricesItem) {
    if (this.prices == null) {
      this.prices = new ArrayList<>();
    }
    this.prices.add(pricesItem);
    return this;
  }

  /**
   * Get prices
   * @return prices
   */
  @javax.annotation.Nonnull
  public List<NetworkFeePreviewPrice> getPrices() {
    return prices;
  }

  public void setPrices(@javax.annotation.Nonnull List<NetworkFeePreviewPrice> prices) {
    this.prices = prices;
  }

  /**
   * A container for additional, undeclared properties.
   * This is a holder for any undeclared properties as specified with
   * the 'additionalProperties' keyword in the OAS document.
   */
  private Map<String, Object> additionalProperties;

  /**
   * Set the additional (undeclared) property with the specified name and value.
   * If the property does not already exist, create it otherwise replace it.
   *
   * @param key name of the property
   * @param value value of the property
   * @return the NetworkFeePreview instance itself
   */
  public NetworkFeePreview putAdditionalProperty(String key, Object value) {
    if (this.additionalProperties == null) {
        this.additionalProperties = new HashMap<String, Object>();
    }
    this.additionalProperties.put(key, value);
    return this;
  }

  /**
   * Return the additional (undeclared) property.
   *
   * @return a map of objects
   */
  public Map<String, Object> getAdditionalProperties() {
    return additionalProperties;
  }

  /**
   * Return the additional (undeclared) property with the specified name.
   *
   * @param key name of the property
   * @return an object
   */
  public Object getAdditionalProperty(String key) {
    if (this.additionalProperties == null) {
        return null;
    }
    return this.additionalProperties.get(key);
  }


  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    NetworkFeePreview networkFeePreview = (NetworkFeePreview) o;
    return Objects.equals(this.feeMode, networkFeePreview.feeMode) &&
        Objects.equals(this.quoteCurrency, networkFeePreview.quoteCurrency) &&
        Objects.equals(this.feeAllowanceCapQuoteMicros, networkFeePreview.feeAllowanceCapQuoteMicros) &&
        Objects.equals(this.prices, networkFeePreview.prices)&&
        Objects.equals(this.additionalProperties, networkFeePreview.additionalProperties);
  }

  @Override
  public int hashCode() {
    return Objects.hash(feeMode, quoteCurrency, feeAllowanceCapQuoteMicros, prices, additionalProperties);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class NetworkFeePreview {\n");
    sb.append("    feeMode: ").append(toIndentedString(feeMode)).append("\n");
    sb.append("    quoteCurrency: ").append(toIndentedString(quoteCurrency)).append("\n");
    sb.append("    feeAllowanceCapQuoteMicros: ").append(toIndentedString(feeAllowanceCapQuoteMicros)).append("\n");
    sb.append("    prices: ").append(toIndentedString(prices)).append("\n");
    sb.append("    additionalProperties: ").append(toIndentedString(additionalProperties)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    return o == null ? "null" : o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("fee_mode", "quote_currency", "fee_allowance_cap_quote_micros", "prices"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(Arrays.asList("prices"));
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to NetworkFeePreview
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!NetworkFeePreview.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field(s) %s in NetworkFeePreview is not found in the empty JSON string", NetworkFeePreview.openapiRequiredFields.toString()));
        }
      }

      // check to make sure all required properties/fields are present in the JSON string
      for (String requiredField : NetworkFeePreview.openapiRequiredFields) {
        if (jsonElement.getAsJsonObject().get(requiredField) == null) {
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field `%s` is not found in the JSON string: %s", requiredField, jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      // validate the optional field `fee_mode`
      if (jsonObj.get("fee_mode") != null && !jsonObj.get("fee_mode").isJsonNull()) {
        FeePolicyModeInputEnum.validateJsonElement(jsonObj.get("fee_mode"));
      }
      // validate the optional field `quote_currency`
      if (jsonObj.get("quote_currency") != null && !jsonObj.get("quote_currency").isJsonNull()) {
        FeePolicyQuoteCurrencyInputEnum.validateJsonElement(jsonObj.get("quote_currency"));
      }
      if ((jsonObj.get("fee_allowance_cap_quote_micros") != null && !jsonObj.get("fee_allowance_cap_quote_micros").isJsonNull()) && !jsonObj.get("fee_allowance_cap_quote_micros").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `fee_allowance_cap_quote_micros` to be a primitive type in the JSON string but got `%s`", jsonObj.get("fee_allowance_cap_quote_micros").toString()));
      }
      if (jsonObj.get("prices") != null) {
        if (!jsonObj.get("prices").isJsonArray()) {
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `prices` to be an array in the JSON string but got `%s`", jsonObj.get("prices").toString()));
        }
        JsonArray jsonArrayprices = jsonObj.getAsJsonArray("prices");
        // validate the required field `prices` (array)
        for (int i = 0; i < jsonArrayprices.size(); i++) {
          NetworkFeePreviewPrice.validateJsonElement(jsonArrayprices.get(i));
        }
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!NetworkFeePreview.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'NetworkFeePreview' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<NetworkFeePreview> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(NetworkFeePreview.class));

       return (TypeAdapter<T>) new TypeAdapter<NetworkFeePreview>() {
           @Override
           public void write(JsonWriter out, NetworkFeePreview value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             obj.remove("additionalProperties");
             // serialize additional properties
             if (value.getAdditionalProperties() != null) {
               for (Map.Entry<String, Object> entry : value.getAdditionalProperties().entrySet()) {
                 if (entry.getValue() instanceof String)
                   obj.addProperty(entry.getKey(), (String) entry.getValue());
                 else if (entry.getValue() instanceof Number)
                   obj.addProperty(entry.getKey(), (Number) entry.getValue());
                 else if (entry.getValue() instanceof Boolean)
                   obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                 else if (entry.getValue() instanceof Character)
                   obj.addProperty(entry.getKey(), (Character) entry.getValue());
                 else {
                   JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                   if (jsonElement.isJsonArray()) {
                     obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                   } else {
                     obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                   }
                 }
               }
             }
             elementAdapter.write(out, obj);
           }

           @Override
           public NetworkFeePreview read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             JsonObject jsonObj = jsonElement.getAsJsonObject();
             // store additional fields in the deserialized instance
             NetworkFeePreview instance = thisAdapter.fromJsonTree(jsonObj);
             for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
               if (!openapiFields.contains(entry.getKey())) {
                 if (entry.getValue().isJsonPrimitive()) { // primitive type
                   if (entry.getValue().getAsJsonPrimitive().isString())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsString());
                   else if (entry.getValue().getAsJsonPrimitive().isNumber())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsNumber());
                   else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsBoolean());
                   else
                     throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The field `%s` has unknown primitive type. Value: %s", entry.getKey(), entry.getValue().toString()));
                 } else if (entry.getValue().isJsonArray()) {
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), List.class));
                 } else { // JSON object
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), HashMap.class));
                 }
               }
             }
             return instance;
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of NetworkFeePreview given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of NetworkFeePreview
   * @throws IOException if the JSON string is invalid with respect to NetworkFeePreview
   */
  public static NetworkFeePreview fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, NetworkFeePreview.class);
  }

  /**
   * Convert an instance of NetworkFeePreview to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}
